const initState = {
  isLoggedIn: true,
  token: 'quang111',
}

const authReducer  = (state = initState, action) => {
  switch(action.type) {

    default:
      return state;
  }
}

export default authReducer ;